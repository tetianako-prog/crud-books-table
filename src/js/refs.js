const container = document.querySelector('.container');
const table = document.querySelector('table');
const addButton = document.querySelector('.add');
const spinner = document.querySelector('.add > .spinner');
const addBtnLabel = document.querySelector('.add-label');

export default {
  container,
  table,
  addButton,
  spinner,
  addBtnLabel,
};
